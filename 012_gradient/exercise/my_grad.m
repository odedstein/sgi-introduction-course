function my_grad(V,F,u,k)
%MY_GRAD Compute the numerical gradient operator for triangle meshes
%
% my_grad(V,F);
%
% Inputs:
%  V,F  the input mesh for which to compute the gradient operator
% Outputs:
%  G  the sparse gradient matrix
%

G = ...

end

