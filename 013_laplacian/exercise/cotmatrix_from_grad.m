function L = cotmatrix_from_grad(V,F)
%COTMATRIX_FROM_GRAD computes the cotangent matrix (Laplacian matrix) for a 
%mesh using the gradient and triangle areas
%
% L = cotmatrix_from_grad(V,F,u,tol);
%
% Inputs:
%  V,F  the input mesh
% Outputs:
%  L  the sparse Laplacian matrix
%

L = ...

end

