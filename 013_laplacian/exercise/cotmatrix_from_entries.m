function L = cotmatrix_from_entries(V,F)
%COTMATRIX_FROM_GRAD computes the cotangent matrix (Laplacian matrix) for a 
%mesh using the cotangent formula.
%
% L = cotmatrix_from_entries(V,F,u,tol);
%
% Inputs:
%  V,F  the input mesh
% Outputs:
%  L  the sparse Laplacian matrix
%

L = ...

end

